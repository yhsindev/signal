import type { Article } from '../../types/article'

interface Props {
  article: Article
  onClose: () => void
}

export default function ArticleDetail(_props: Props) {
  // Phase 2: 顯示 Claude 摘要、值不值得深讀判斷、個人觀點輸入、消化狀態更新
  return null
}
